namespace Fiar
{
    /// <summary>
    /// Role defining what is the game results
    /// </summary>
    public enum GameResult
    {
        Draw = 0,
        PlayerOneWon = 1,
        PlayerTwoWon = 2,
    }
}
