namespace Fiar
{
    /// <summary>
    /// Role defining what is the game results
    /// </summary>
    public enum GameResult
    {
        None = 0, // Currently in progress
        PlayerOneWon = 1,
        PlayerTwoWon = 2,
        Draw = 3,
    }
}
