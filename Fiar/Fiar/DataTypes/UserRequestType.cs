namespace Fiar
{
    /// <summary>
    /// Request type that user can perform
    /// </summary>
    public enum UserRequestType
    {
        Friend = 0,
        Challange = 1,
        AcceptChallange = 2,
    }
}
