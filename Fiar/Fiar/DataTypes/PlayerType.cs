namespace Fiar
{
    /// <summary>
    /// Role defining what is the player role in a game
    /// </summary>
    public enum PlayerType
    {
        Spectator = 0,
        PlayerOne = 1,
        PlayerTwo = 2,
    }
}
